# Dependências de desenvolvimento (não instalar em produção)
# Instalar com: pip install -r requirements-dev.txt

# Test
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2           # TestClient async

# Linting / formatting
ruff==0.7.0

# Type checking
mypy==1.11.0

# Includes todas as deps de produção
-r requirements.txt
